configserver: true
bus: true

eureka:    
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eurekaserver.credentials.uri:http://127.0.0.1:8680}/eurekaserver/eureka/
ribbon:
  eureka:
    enabled: true

---

spring:
  profiles: cloud
eureka:
  instance:
    hostname: ${vcap.application.uris[0]}
    nonSecurePort: 80